{
  "$comment": "DESIGN PROTOTYPE — stays a prototype. The v1 declarative schema mechanism now exists (host.contribute.schemaPanel — see packages/draw-bundle/src/panels/stroke-panel.ts and BREAKAGE_LOG.md B-01 RESOLVED); this panel can adopt it once gradient assignment is verified (B-03): swatch fills bind today via frameFillColor colorRef, but whether a Gradient/* self-id is accepted there is unverified, so the gradient rows would be honest seams. Convert to a SchemaPanelContribution when B-03 lands.",
  "panel": "media.paged.draw.panel.fill",
  "title": { "key": "panel.fill.title" },
  "icon": "icons/fill.svg",
  "sections": [
    {
      "rows": [
        {
          "type": "colorWell",
          "bind": { "kind": "selectionProperty", "path": "frameFillColor" },
          "label": { "key": "fill.color" }
        },
        {
          "type": "numeric",
          "bind": { "kind": "selectionProperty", "path": "frameFillTint", "coerce": "%" },
          "min": 0,
          "max": 100,
          "label": { "key": "fill.tint" }
        }
      ]
    },
    {
      "title": { "key": "fill.gradient" },
      "$comment": "On-canvas annotator angle/length ride existing paths once assignment works.",
      "rows": [
        {
          "type": "numeric",
          "bind": { "kind": "selectionProperty", "path": "frameGradientFillAngle" },
          "label": { "key": "gradient.angle" }
        },
        {
          "type": "numeric",
          "bind": { "kind": "selectionProperty", "path": "frameGradientFillLength", "coerce": "pt" },
          "label": { "key": "gradient.length" }
        }
      ]
    }
  ]
}
