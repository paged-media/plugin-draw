{
  "$comment": "SUPERSEDED PROTOTYPE — the real panel ships: see packages/draw-bundle/src/panels/fill-panel.ts (a SchemaPanelContribution contributed through host.contribute.schemaPanel). B-03 is verified engine-side (a Gradient/* self-id IS accepted at frameFillColor — pinned by test/conformance/gradient-fill.spec.ts), so the gradient section is real: its visibility is a PUBLISHED binding (gradientControlsVisible) the bundle derives from the selection's fill ref, and gradient ASSIGNMENT is command-driven (Fill: Linear/Radial gradient — a multi-mutation flow above the scalar binding ceiling, the dash precedent). This JSON form is kept only as the historical design record.",
  "panel": "media.paged.draw.panel.fill",
  "title": { "key": "panel.fill.title" },
  "icon": "icons/fill.svg",
  "sections": [
    {
      "rows": [
        {
          "type": "colorWell",
          "bind": { "kind": "selectionProperty", "path": "frameFillColor" },
          "label": { "key": "fill.color" }
        },
        {
          "type": "numeric",
          "bind": { "kind": "selectionProperty", "path": "frameFillTint", "coerce": "%" },
          "min": 0,
          "max": 100,
          "label": { "key": "fill.tint" }
        }
      ]
    },
    {
      "title": { "key": "fill.gradient" },
      "$comment": "On-canvas annotator angle/length ride existing paths once assignment works.",
      "rows": [
        {
          "type": "numeric",
          "bind": { "kind": "selectionProperty", "path": "frameGradientFillAngle" },
          "label": { "key": "gradient.angle" }
        },
        {
          "type": "numeric",
          "bind": { "kind": "selectionProperty", "path": "frameGradientFillLength", "coerce": "pt" },
          "label": { "key": "gradient.length" }
        }
      ]
    }
  ]
}
